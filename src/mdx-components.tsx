// Required by `@next/mdx` — Next.js looks for `mdx-components.tsx` at the
// project root (or `src/` root). Re-exports the editorial component map.
export { useMDXComponents } from "@/components/mdx/mdx-components";
